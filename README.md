# Simple-RAG-Architecture
RAG (Retrieval Augmented Generation) addresses a common concern: does an LLM store your personal data? It works by converting documents into chunks, then into embeddings (vector representations). Using cosine similarity, only the chunks relevant to your question are sent to the LLM.
